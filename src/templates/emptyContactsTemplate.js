import _ from 'underscore'

const template = `
	<div class="well text-center"><h3>There is no contacts.</h3> <a href="#new" class="btn btn-lg btn-outline">Add Contact</a></div>
`
export default _.template(template)
